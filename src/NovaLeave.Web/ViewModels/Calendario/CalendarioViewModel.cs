using NovaLeave.Application.Calendars;

namespace NovaLeave.Web.ViewModels.Calendario;

public sealed record CalendarioViewModel(CalendarViewModel Calendar);

public sealed record CalendarioAprobadorViewModel(CalendarViewModel Calendar);
