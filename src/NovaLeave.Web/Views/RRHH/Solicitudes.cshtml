@model NovaLeave.Web.ViewModels.RRHH.RRHHSolicitudesIndexViewModel
@{
    ViewData["Title"] = "Solicitudes RRHH";
}

<div class="nl-page-header">
  <div>
    <p class="nl-page-kicker">RRHH</p>
    <h1 class="nl-page-title">Solicitudes</h1>
    <p class="nl-page-description">Consulta organizacional de solicitudes de vacaciones.</p>
  </div>
  <div class="d-flex flex-wrap gap-2">
    <span class="nl-badge nl-readonly">Solo lectura</span>
    <a class="btn btn-outline-secondary" href="/rrhh">Volver</a>
  </div>
</div>

<section class="nl-card">
  <div class="nl-card-body">
<div class="table-responsive">
  <table class="table nl-table nl-table-card align-middle">
    <caption>Solicitudes de vacaciones de la organizacion</caption>
    <thead>
      <tr>
        <th scope="col">Solicitante</th>
        <th scope="col">Inicio</th>
        <th scope="col">Fin</th>
        <th scope="col">Dias</th>
        <th scope="col">Estado</th>
        <th scope="col">Detalle</th>
      </tr>
    </thead>
    <tbody>
      @foreach (var request in Model.Requests.Items)
      {
        <tr>
          <td>@request.RequesterName</td>
          <td>@request.StartDate.ToString("yyyy-MM-dd")</td>
          <td>@request.EndDate.ToString("yyyy-MM-dd")</td>
          <td>@request.WorkingDays</td>
          <td><partial name="_StatusBadge" model="request.Status.ToString()" /></td>
          <td><a class="btn btn-sm btn-outline-primary" href="/rrhh/solicitudes/@request.Id">Ver</a></td>
        </tr>
      }
    </tbody>
  </table>
</div>
  </div>
</section>

<p class="text-body-secondary">Pagina @Model.Requests.Page de @Math.Max(1, (int)Math.Ceiling(Model.Requests.TotalCount / (double)Model.Requests.PageSize))</p>
