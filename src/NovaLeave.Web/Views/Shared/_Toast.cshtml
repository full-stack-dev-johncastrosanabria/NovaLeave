@model string

@if (!string.IsNullOrWhiteSpace(Model))
{
  <div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div class="toast show nl-card" role="status" aria-live="polite" aria-atomic="true">
      <div class="toast-body">@Model</div>
    </div>
  </div>
}
